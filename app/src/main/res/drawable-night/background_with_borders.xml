<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/background_with_borders_item">
        <shape android:shape="rectangle">
            <solid android:color="@color/white_or_black_primary_total" /> <!-- Transparent color -->
            <corners android:radius="8dp" /> <!-- Corner radius -->
            <stroke android:color="@color/white" android:width="0.5dp"/> <!-- Border color and width -->
        </shape>
    </item>
</layer-list>
